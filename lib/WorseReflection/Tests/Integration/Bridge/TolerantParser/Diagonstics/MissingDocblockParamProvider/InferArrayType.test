<?php

class Foobar
{
    /**
     */
    public function foo(array $foobars): array {
         foreach ($foobars as $foobar) {
             $this->add($foobar);
         }

         return $foobars;
    }

    private function add(string $foobar) {
    }
}


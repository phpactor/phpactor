<?php

function create(string $source): TextDocument
{
}

function fromSource($source): void
{
    if (!$source instanceof TextDocument) {

        $source = create($source);

    }
    wrAssertType('TextDocument', $source);
}

<?php

if (in_array($foo, ['foo', 'bar'])) {
    wrAssertType('"foo"|"bar"', $foo);
}

/** @var string */
$foo;

$bars = ['foo', 'bar'];
if (in_array($foo, $bars)) {
    wrAssertType('"foo"|"bar"', $foo);
    die();
}
wrAssertType('string', $foo);



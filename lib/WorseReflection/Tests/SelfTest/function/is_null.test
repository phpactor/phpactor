<?php

function foo()
{
}

assert(is_null($foo));
wrAssertType('null', $foo);

assert(!is_null($foo));
wrAssertType('<missing>', $foo);

// wrAssertType('true', is_null(null));
// wrAssertType('false', is_null("fo"));
// 
// $foo = foo();
// 

